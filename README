This is a small, but computationally hard module, written on python 3 and using OpenCV for denoising videos and images.
Mostly all variables you may need are at the top of the .py file, so as usefull comments there. 